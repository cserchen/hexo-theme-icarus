<% if (theme.links && Object.keys(theme.links).length){ %>
  <div class="widget-wrap widget-list">
    <h3 class="widget-title"><%= __('widget.links') %></h3>
    <div class="widget">
      <ul>
        <% for (var i in theme.links){ %>
          <li>
            <a href="<%- theme.links[i] %>"><%= i %></a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
<% } %>
